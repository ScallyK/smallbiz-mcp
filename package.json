{
  "name": "smallbiz-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for small business operations (calendar, CRM, sales, marketing)",
  "private": true,
  "type": "module",
  "bin": {
    "smallbiz-mcp-server": "./build/mcp.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/mcp.js', '755')\"",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "inspector": "npx @modelcontextprotocol/inspector build/mcp.js",
    "dev": "npx nodemon --watch src --ext ts --exec \"node --loader ts-node/esm src/mcp.ts\""
  },
  "dependencies": {
    "@google-cloud/local-auth": "^2.1.0",
    "@modelcontextprotocol/sdk": "^1.17.4",
    "@prisma/client": "^6.14.0",
    "dotenv": "^17.2.1",
    "googleapis": "^105.0.0",
    "libphonenumber-js": "^1.12.15",
    "square": "^43.0.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.19.11",
    "nodemon": "^3.1.10",
    "prisma": "^6.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
